name: Run tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
    steps:
      - name: Create .env file
        run: |
          touch .env
          echo TOKEN = ${{ secrets.TOKEN }} >> .env